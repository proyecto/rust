// src/constants.rs

// Dimensiones de la ventana principal
pub const WINDOW_WIDTH: f64 = 800.0;
pub const WINDOW_HEIGHT: f64 = 600.0;

// Dimensiones de la barra lateral
pub const SIDEBAR_WIDTH: f64 = 200.0;

// Colores
pub const LEFT_VIEW_COLOR: (f32, f32, f32) = (0.8, 0.8, 0.8); // Gris
pub const RIGHT_VIEW_COLOR: (f32, f32, f32) = (1.0, 1.0, 1.0); // Blanco